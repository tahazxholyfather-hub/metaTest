import React from 'react';
import  { ArrowUpRight, ArrowDownRight } from 'lucide-react';

interface StatCardProps {
    title: string;
    value: string;
    trend: string;
    trendUp: boolean;

}

export default function StatCard({ title, value, trend, trendUp, icon: Icon }: StatCardProps) {
    return (
        <div className="p-5 rounded-2xl bg-white dark:bg-[#1c1c1e] border border-[#e5e5ea] dark:border-[#2d3748] shadow-sm hover:shadow-md transition-shadow duration-200 group">
            <div className="flex items-center justify-between mb-4">
                <span className="text-sm font-medium text-[#86868b] dark:text-[#9ca3af]">{title}</span>
                <div className="p-2 rounded-lg bg-[#f5f5f7] dark:bg-[#2d3748] group-hover:bg-blue-50 dark:group-hover:bg-blue-500/10 transition-colors">
                    <Icon className="w-4 h-4 text-blue-500 dark:text-blue-400" />
                </div>
            </div>

            <div className="flex flex-col gap-1">
                <span className="text-3xl font-semibold tracking-tight text-[#1c1c1e] dark:text-[#f5f5f7]">
                    {value}
                </span>
                <div className="flex items-center gap-1.5 text-xs font-medium mt-1">
                    <span className={`flex items-center ${trendUp ? 'text-emerald-600 dark:text-emerald-400' : 'text-red-600 dark:text-red-400'}`}>
                        {trendUp ? <ArrowUpRight className="w-3.5 h-3.5 mr-0.5" /> : <ArrowDownRight className="w-3.5 h-3.5 mr-0.5" />}
                        {trend}
                    </span>
                    <span className="text-[#86868b]">vs last month</span>
                </div>
            </div>
        </div>
    );
}
