import React from 'react';
import { Menu, Moon, Sun, Bell } from 'lucide-react';

interface HeaderProps {
    toggleSidebar: () => void;
    isDarkMode: boolean;
    toggleDarkMode: () => void;
}

const Header: React.FC<HeaderProps> = ({ toggleSidebar, isDarkMode, toggleDarkMode }) => {
    return (
        <header className="h-20 flex items-center justify-between px-4 lg:px-8 shrink-0 relative z-30">
            <div className="flex items-center">
                <button
                    onClick={toggleSidebar}
                    className="lg:hidden w-10 h-10 flex items-center justify-center rounded-full bg-white/50 dark:bg-[#202123]/40 backdrop-blur-xl border border-[#1c1c1e]/5 dark:border-[#f5f5f7]/5 text-[#86868b] active:scale-95 transition-all"
                >
                    <Menu size={18} />
                </button>

                {/* Subtle Breadcrumb/Context Indicator */}
                <div className="hidden lg:flex flex-col ml-2">
                    <span className="text-[10px] uppercase tracking-[0.2em] text-[#86868b] font-medium">Workspace</span>
                    <span className="text-sm font-medium text-[#1c1c1e] dark:text-[#f5f5f7]">Admin Console</span>
                </div>
            </div>

            <div className="flex items-center gap-2 bg-white/50 dark:bg-[#202123]/40 backdrop-blur-xl border border-[#1c1c1e]/5 dark:border-[#f5f5f7]/5 rounded-full p-1.5 shadow-[0_2px_10px_rgba(0,0,0,0.02)]">
                <button className="relative p-2.5 rounded-full text-[#86868b] hover:text-[#1c1c1e] dark:hover:text-[#f5f5f7] hover:bg-[#1c1c1e]/5 dark:hover:bg-[#f5f5f7]/5 transition-all">
                    <Bell size={16} strokeWidth={2} />
                    <span className="absolute top-2.5 right-2.5 w-1.5 h-1.5 bg-[#1c1c1e] dark:bg-[#f5f5f7] rounded-full"></span>
                </button>

                <div className="w-px h-4 bg-[#e5e7eb] dark:bg-[#2d3748] mx-1"></div>

                <button
                    onClick={toggleDarkMode}
                    className="p-2.5 rounded-full text-[#86868b] hover:text-[#1c1c1e] dark:hover:text-[#f5f5f7] hover:bg-[#1c1c1e]/5 dark:hover:bg-[#f5f5f7]/5 transition-all"
                >
                    {isDarkMode ? <Sun size={16} strokeWidth={2} /> : <Moon size={16} strokeWidth={2} />}
                </button>
            </div>
        </header>
    );
};

export default Header;
